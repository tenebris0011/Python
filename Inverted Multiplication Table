n=int(10) 
for row in range(n,-1,-1): #n is 10, the first -1 tells the script where to stop. The second -1 tells the script to do n-1 for the next number
	print(*("{:3}".format(row*col) for col in range(n,-1,-1))) #This prints out the rows and columns and does row*col for each number from 10. the "{:3}" defines spacing.
