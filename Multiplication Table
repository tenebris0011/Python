n=int(10) #This defines the n as an int that equals 10.
for row in range(0,n+1): #This does n+1 for everything from 0
	print(*("{:3}".format(row*col) for col in range(0, n+1))) #This prints out the rows and columns and does row*col for each number from 0 in the equation n+1. the "{:3}" defines spacing.
